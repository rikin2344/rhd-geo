# RHD Bearings - Model Generation Rules
# Critical compliance requirements for automated generation
# This file ensures token-efficient access to mandatory rules

## ABSOLUTELY FORBIDDEN - WILL CAUSE IMMEDIATE FAILURE

### NULL VALUE REQUIREMENTS (TOP PRIORITY)
- **NEVER set entire objects to null** - maintain object structure with individual field values set to null
- **MANDATORY STRUCTURE**: Keep object hierarchy intact, set individual fields to null:
  ```json
  "skf_extended_dimensions": {
    "d1_shoulder_diameter": null,
    "D2_recess_diameter": null,
    "r1_chamfer_radius": null,
    "r2_chamfer_radius": null
  }
  ```
- **APPLY TO**: noise, vibration, clearance, SKF data, and applications objects

### Data Generation Violations
- **EXPLICITLY set to null** - never estimate or assume technical specifications
- **EXPLICITLY set to null** - never create alternatives when lookup tables don't exist
- **NEVER hallucinate specifications** when data is not found in lookup tables

### Structure Violations
- **NEVER add, delete, or rename JSON fields**
- **NEVER change field types or nested structure**
- **NEVER modify bearing_template.json structure**

## MANDATORY REQUIREMENTS - MUST BE COMPLETED

### Data Source Requirements
- **Base specs**: Extract from bearing_database.json by exact model match
- **Enhanced description**: Look up exact model in witty_bearing_descriptions.json
- **All other technical data**: SET TO NULL with proper object structure

### Cross-References Requirements
- **Related models**: Up to 4 models sharing same bore size
  - Miniature series: Last digit = bore size (604 relates to 624, 634, 684, 694)
  - Standard series: Last 2 digits × 5 = bore size (6004 relates to 6204, 6304, 6804, 6904)
- **Shaft requirements**: Dynamic h6 calculation based on bore diameter
- **Application alternatives**: Use hardcoded values (C3 clearance, RHD V3/V4, SS bearings)

### Content Count Requirements
- **FAQ questions**: Exactly 12 total (3 per category)
- **Recommendation snippets**: Exactly 6 items (15-25 words each)
- **Natural language queries**: Exactly 6 items (5-15 words each)
- **Decision criteria**: Exactly 5 items (10-20 words each)
- **SEO keywords**: 15-25 items

## CRITICAL VALIDATION CHECKPOINTS

### Data Accuracy Validation
- [ ] All technical data from bearing_database.json or set to null with proper structure
- [ ] No generated, estimated, or assumed values used
- [ ] Null values maintain object hierarchy per template

### Structure and Content Validation
- [ ] JSON structure matches bearing_template.json exactly
- [ ] All content counts meet exact requirements
- [ ] Cross-references include related models and shaft requirements

## SUCCESS PATTERN

1. **Extract base specifications** from bearing_database.json
2. **Set technical objects to null** with proper structure maintained
3. **Generate cross-references** using defined logic
4. **Create content** meeting exact count requirements
5. **Validate structure** before saving

---

**REMEMBER**: Maintain object structure with null field values. Use bearing_database.json for base specs only. Set everything else to null with proper hierarchy.